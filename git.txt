API https://core.telegram.org/bots/api#available-methods

npm init
npm install node-telegram-bot-api
npm install telegraf --save
npm install axios --save
npm install -g nodemon
npm install --save-dev nodemon

process.env["NTBA_FIX_319"] = 1;


git remote rm orirgin удалить 
git remote -v URL соответствует сокращённое имя в Git

rm .git/index  https://thoughtbot.com/blog/how-to-fix-rm-f-git-index

git pull origin master --allow-unrelated-histories 
git push origin master

последний коммит git branch -v: 

git config http.postBuffer 524288000  увеличить репозиторий (буфер)

git config --global gui.encoding utf-8   кодировка для всех репозиториев
git config gui.encoding utf-8     для одного

error:403
git config --global --add remote.origin.proxy ""
git config --global http.sslBackend "openssl"

utf-8
...